class Solution:
    def countElements(self, nums):
        smallest = min(nums)
        largest = max(nums)

        count = 0
        for n in nums:
            if smallest < n < largest:
                count += 1

        return count